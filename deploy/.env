# ============================================================
#  Example environment file for S4C Microservices Architecture
#  Copy this file to ".env" and customize the values.
# ============================================================

# Flask secret key for session signing.
# Generate a strong key using:
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me

# Optional: mobility API URL override (if not using Docker Compose)
# MOBILITY_API_URL=http://localhost:8081/predict

# Optional: database file paths (only needed for advanced setups)
# USERS_DB=backend/db/utenti.db
# PARKING_DB=backend/db/parcheggi.db
# SIM_DB=backend/db/simulazioni.db
# LINES_DB=backend/db/linee.db